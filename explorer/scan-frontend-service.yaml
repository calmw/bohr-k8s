apiVersion: v1
kind: Service
metadata:
  name: scan-frontend
  namespace: bohr-test
spec:
  type: ClusterIP
  selector:
    app: scan-frontend
  ports:
    - name: http
      port: 3000        # Service 在集群内暴露的端口
      targetPort: 3000  # Pod 内应用实际监听的端口 (根据日志是3000)
